#include<stdio.h>

int main(){
  
  int a, b;
  int max;
  printf("두 수를 입력하시오:");
  scanf_s("%d %d", &a, &b);

  if ( a>= b)
    max = a;
  else 
    max = b;

  printf("최대값 = %d \n", max);
  return 0;
}
